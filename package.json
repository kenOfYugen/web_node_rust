{
  "name": "webrust",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf node_modules/ target/ build/",
    "build": "coffee -b --no-header -o target/ -c src/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bindings": "^1.3.0",
    "coffeescript": "^2.0.1",
    "node-gyp": "^3.6.2"
  }
}
